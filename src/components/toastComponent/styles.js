import Styled from "styled-components";

export const ToastComponentWrapper = Styled.div``;
export const ToastComponentHeading = Styled.div`
  margin-bottom: 4px;
  font-size: 14px;
  font-weight: bold;
`;

export const ToastComponentMessage = Styled.div`
  line-height: 1.5;
  font-size: 12px;
  .message {
    font-weight: 900;
  }
`;
